// 2 types of datatypes 
// Primitive and Non Primitive

// The main difference between them is that 
// Primitive are 'call by value' && Non-Prim are 'call by refrence'

// Primitive: String, Number, Boolean, null, undfined, Symbol, BigInt
// Non-Prim: Array, Object, Function

// lets study 'Symbol'
// the point of symbol is that it distinguishes b/w two var even if you give them the same value

// a = Symbol('123');
// b = Symbol('123');

// console.log(a);
// console.log(b);

// console.log(a == b); // false




